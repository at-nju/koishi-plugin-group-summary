# 用 Pages 托管界面并用 R2 发布数据快照

Cloudflare Pages 只托管绑定自定义域名的网页外壳，Koishi 所在环境周期性地将版本化发布快照上传到 R2，网页在打开或刷新时读取最新完整快照。R2 只保存网页实际读取的数据，不保存本地内容库、未发布消息或图片文件；这样不需要为本地环境提供公网入口，也不为每批群聊数据创建一次 Pages 部署。这个选择接受批次延迟和远端对象存储依赖，以换取简单、原子化的公开发布。
